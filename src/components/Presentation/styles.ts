import { SxProps } from '@mui/material';


const styles: Record<string, SxProps> = {
   slider: {
      zIndex: '100',
      position: 'fixed',
      top: '0',
      left: '0',
      width: '100%',
      height: '100vh',
   }
}

export default styles;